name: Terraform Destroy Cron

on:
  schedule:
    - cron: '0 1 * * *' # Must match with apply +1h
    - cron: '0 7 * * *' # Must match with apply +1h

jobs:
  destroy:
    uses: ./.github/workflows/terraform-destroy.yaml
    secrets: inherit
