name: Terraform Apply Cron

on:
  schedule:
    - cron: '0 0 * * *' # Midnight UTC. Must match with destroy
    - cron: '0 6 * * *'
  push:
    tags:
      - daily_apply

jobs:
  apply:
    uses: ./.github/workflows/terraform-apply.yaml
    with:
      subfolder: "daily"
    secrets: inherit
